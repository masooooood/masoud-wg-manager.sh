🛡 Masoud WireGuard Manager (v2)
مدیریت حرفه‌ای و نیمه‌خودکار WireGuard برای سناریوی ایران ↔ خارج (Site-to-Site)



## 🚀 Quick Install

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/masooooood/masoud-wg-manager.sh/main/masoud-wg-manager.sh | sed 's/\r$//')
```

---
✨ امکانات

نصب خودکار WireGuard و پیش‌نیازها

ساخت خودکار کلیدهای ایران و خارج

Remote Bootstrap (تنظیم خودکار سرور خارج از طریق SSH)

مدیریت کامل سرویس‌ها (Start / Stop / Restart / Logs / Remove)

جلوگیری از خطای wg0 already exists

اصلاح خودکار مشکلات رایج systemd

امکان تغییر سریع پورت (مثلاً 443/udp برای تست)

قابلیت اجرا هم روی ایران و هم روی خارج


📌 نحوه استفاده (سناریوی پیشنهادی)
فقط روی سرور ایران اجرا کن:

از منو گزینه:

2) Setup as IRAN (recommended)

را انتخاب کن.

اسکریپت:

کلیدها را می‌سازد

wg0 را روی ایران تنظیم می‌کند

از طریق SSH وارد سرور خارج می‌شود

WireGuard را روی خارج نصب و کانفیگ می‌کند

سرویس‌ها را استارت می‌کند

نیازی به ورود دستی به خارج نیست.

🔧 اگر خواستی روی خارج جداگانه اجرا کنی

گزینه:

3) Setup as KHAREJ (local only / repair)

برای تعمیر یا تنظیم دستی روی خارج استفاده می‌شود.

📊 مشاهده وضعیت تونل
4) Status

یا مستقیم:

wg show
🛠 مدیریت سرویس‌ها
5) Service Manager

قابلیت‌ها:

لیست سرویس‌های wg

Start / Stop / Restart

مشاهده لاگ

حذف کامل اینترفیس

🔄 تغییر سریع پورت (مثلاً تست 443/udp)
6) Change Port helper
⚠ نکته مهم درباره UDP در ایران

WireGuard بر پایه UDP است.
اگر handshake برقرار نمی‌شود یا received = 0 می‌بینید:

ممکن است UDP برگشتی در مسیر ایران فیلتر شود

پورت را به 443 تغییر دهید

یا از روش‌های TCP-based استفاده کنید

📁 مسیر فایل‌ها
مورد	مسیر
کلیدها	/etc/masoud-wg/keys/
کانفیگ WireGuard	/etc/wireguard/wg0.conf
سرویس
